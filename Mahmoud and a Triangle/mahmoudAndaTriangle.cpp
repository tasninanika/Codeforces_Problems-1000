#include<bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin >> n;

    while(cin >> n){
        vector<int> v(n);

        for (i=0; i<n; i++)
        {
            cin >> x;
            v.push_back(x);
        }

        sort(v.begin(), v.end());

    }

    return 0;
}
